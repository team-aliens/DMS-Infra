upstream dms_gateway {
    server gateway:8080;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 50m;

    location / {
        proxy_pass         http://dms_gateway;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}

# ---- HTTPS (인증서 발급 후 주석 해제) ----
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name dms.example.kr;
#
#     ssl_certificate     /etc/letsencrypt/live/dms.example.kr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dms.example.kr/privkey.pem;
#
#     client_max_body_size 50m;
#
#     location / {
#         proxy_pass         http://dms_gateway;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#     }
# }
